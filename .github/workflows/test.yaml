name: Test

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install Dagger
        run: curl -fsSL https://dl.dagger.io/dagger/install.sh | sh

      - name: Run tests
        working-directory: server
        run: ./bin/dagger --progress plain call -m ci test --source .

      - name: Build all targets
        working-directory: server
        run: ./bin/dagger --progress plain call -m ci build --source .
