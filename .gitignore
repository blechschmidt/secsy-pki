# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars
terraform/.terraform.lock.hcl

# Built provider
terraform-provider-pkcs11/

# Server
server/secsy-pki.db
server/secsy-pki.db-shm
server/secsy-pki.db-wal
server/secsy-pki-server
server/secsy-ca
# Generated by the YubiHSM PKCS#11 module bootstrap when a connector is configured.
server/yubihsm_pkcs11.conf
yubihsm_pkcs11.conf
server/certs/

# Binaries
*.exe

# Legacy names
server/ssh-pki-server
server/ssh-pki.db*

# Orchestration / local state
.cloop/

# Supply-chain build output (SBOMs, provenance) — see Makefile / docs/development/supply-chain.md
dist/
# Local cosign keys must never be committed.
cosign.key
cosign.pub

# stray go build output in server/
server/server

# Python bytecode (mkdocs imports website/hooks.py when building the docs site)
__pycache__/
